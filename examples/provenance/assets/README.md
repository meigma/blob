# Example Assets

This directory contains static files that are archived and pushed to an OCI registry
as a blob archive.

## Contents

- `config.json` - Application configuration
- `data/sample.txt` - Sample data file

## Usage

These files are archived using the `provenance push` command and can be pulled
with full provenance verification using `provenance pull`.
